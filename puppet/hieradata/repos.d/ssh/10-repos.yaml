# Repositories required by classes that are added to the classes array by the
# default configuration.

repodeploy::repos:
  '/opt/puppet-modules/ssh':
    source: https://github.com/autostrap/puppet-ssh.git
    provider: git
  '/opt/puppet-modules/base':
    source: https://github.com/autostrap/puppet-base.git
    provider: git
